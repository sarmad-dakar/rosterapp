import { StyleSheet, Text, View } from 'react-native';
import React from 'react';

type Props = {};

const RosterScreen = (props: Props) => {
  return (
    <View>
      <Text>RosterScreen</Text>
    </View>
  );
};

export default RosterScreen;

const styles = StyleSheet.create({});
